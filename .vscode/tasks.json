{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Typescript run current file",
            "command": "npx ts-node ${file}",
            "type": "shell",
            "group": {
                "kind": "none"
            },
            "problemMatcher": "$tsc"
        }
    ]
}
