{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "C++ run file",
      "command": "g++-11 -std=c++11 -o app -I ${fileDirname} ${fileDirname}/*.cpp && ./app && rm app",
      "type": "shell",
      "group": {
        "kind": "none"
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}
