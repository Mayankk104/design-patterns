{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug current file",
            "type": "bun",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
